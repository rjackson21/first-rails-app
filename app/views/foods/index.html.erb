
<div class= "p-5 bg-light" style="text-align:center;">

<p> Questioning if an ingredient is vegan? </p>

<div>
<%= form_tag url: "/foods", method: :post do |form| %>
  <%= text_field_tag :ingredient, nil, class: "form-control", placeholder: "Soy", autofocus: true%>
  <%= submit_tag "Enter", class:"btn btn-primary" %>
<% end %> 
</div>
</div>



<% if @food != nil %>
<table class="table table-hover" style="text-align:center;">
  <thead>
    <tr>
      <th scope="col">Food</th>
      <th scope="col">Vegan</th>
      <th scope="col">Options</th>
    </tr>
  </thead>
  <tbody>
  <% @foods.each do |food| %>
    <tr>
      <td><%= food.name.capitalize %></td>
      <td><%= food.is_vegan.to_s.capitalize %></td>
      <td><%= link_to 'Show', food_path(food.id)%>
      <%= link_to 'Edit', edit_food_path(food.id)%>
      <%= link_to 'Delete', food_path(food.id), method: :delete, remote: true %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<% else %>

<% end %>
<div class= "p-3 bg-light">
<p style="text-align:center;"> "Is it Vegan" helps you identify if an ingredient is vegan on the fly. Beyond the obvious ingredients that are not vegan, foods often include unfamiliar 
ingredients that are questionable to those who are following a vegan diet. Please use at your own risk. The API 
will default to "true" if there is not an exact match.
</p>
</div>



